#!/usr/bin/python3

# Looping from 97 to 122, which are the ASCII values of 'a' to 'z'
for i in range(97, 123):
    # Printing each character on the same line without new lines
    print(chr(i), end='')
